/**
 * Svelte context keys used across the app.
 *
 * These are string literals consumed by `setContext` / `getContext` in
 * individual components. Keeping them in a single module avoids drift and
 * typos.
 */

export const clientContext = 'AppClient';
