{
  "name": "edet-civitas-player",
  "version": "0.7.1",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-or-later",
  "description": "A player over an edet run's tape: scrub the days, watch the stake graph, read what anybody was thinking",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "svelte": "^4.2.19",
    "vite": "^5.4.10"
  }
}
