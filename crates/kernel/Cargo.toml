[package]
name = "edet-kernel"
description = "Deterministic economic kernel: reliability, circulation credit, capacity, governor"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
libm = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
